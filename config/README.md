# Config Profiles

`config/profiles/` contains lightweight local override snippets.

These files are not intended to be complete standalone environment files for deployment.
Start from `.env.example`, then copy values from one of these snippets when you want a smaller local setup preset.

For complete operator-facing env files, use `deploy/profiles/`.
